//Extrayendo caracteres de un strings
 
`
at(position) 
charAt(position)
string[0] 
`

let text = "holaSoyUnString";

// Extrayendo string[0]

let a = text[1];
console.log(a);

// string.charAt()
let b = text.charAt(1);
console.log(b);

// string.at()
let c = text.at(1);
console.log(c);


// Extrayendo partes de un string
`
    slice(start, end)
    substring(start, end)
    substr(start, length)

`

let d = text.slice(0,4);
console.log(d);



